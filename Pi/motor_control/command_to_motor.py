"""
Version 0.0.1
12/22/15
@author: Ben Camp

This sends the command to the motor controller via a particularly formatted byte array
"""
 

def command_to_motor(command_string):
        #this.serialPort.write(byteArrayToWrite);
        #catch (SerialPortException ex)
            #Logger.getLogger(mainController.class.getName()).log(Level.SEVERE, null, ex);
            
# End command_to_motor